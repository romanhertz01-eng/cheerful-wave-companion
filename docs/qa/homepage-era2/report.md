# Recon: главная ЭРА2 после перевода на SEO-конструктор

- Base URL: `http://localhost:8080` (dev preview)
- Viewport: 1440×900, режим гостя (чистый контекст без `era2_auth`)
- Артефакты: `home_guest_full.png`, `home_guest.txt`, `aiimage_footer.png`, `_recon.json`

## Чек-лист

- [x] **Hero**: H1 «90+ нейросетей в одной подписке», 4 таба (Изображение/Видео/Текст/Аудио), textarea, чип модели (`Nano Banana 2`), кнопка «Создать», счётчик `0/2000` — всё на месте.
- [x] **Переключение табов** меняет placeholder и модель:
  - Изображение → «Опишите картинку, которую хотите создать…», чип `Nano Banana 2`
  - Видео → «Опишите сцену для видео…» (клик подтверждён)
- [x] **Порядок секций** сверху вниз:
  1. Hero
  2. Популярные инструменты — 14 карточек (Генератор изображений, Создать видео, Оживить фото, Удалить фон, Апскейл, Реставрация фото, Раскрасить ч/б фото, Озвучка текста, Клонирование голоса, Транскрибация, Удалить объект, Удалить водяной знак, Говорящий аватар, Видео для карточки Ozon)
  3. Модели внутри подписки — 6 карточек с бейджами (Nano Banana 2 `NEW`, Midjourney `TOP`, Seedance 2.0 `NEW`, GPT-5, Claude, ElevenLabs)
  4. «Одна подписка вместо десяти» — editorial-текст
  5. Тарифы — 4 карточки с ценами в ₽ (Базовый 790, Про 1 490, Макс 3 990, Ультра 9 990) + ссылка «Сравнить все тарифы →»
  6. «Сделано в ЭРА2» — галерея без имён/лайков
  7. «Важные вопросы» — 8 вопросов
  8. Финальный CTA
  9. Мега-футер
- [x] **Хлебных крошек на главной НЕТ** (`bc_visible = 0`).
- [x] **Футер ровно один**: `/` → 1, `/ai/image` → 1.
- [x] **От старой главной ничего не торчит**: бегущей строки моделей, «Дизайн и креативность», «Текстовые ассистенты», QuickActions — не найдено (grep пустой).
- [x] **Клик по «Оживить фото»**: ссылка ведёт на `/tools/ozhivit-foto` (`href` подтверждён).
- [x] **Кнопка «Создать» гостем**: ведёт на `/auth?next=%2Fvideo` (активный таб «Видео» после клика; параметр `next` присутствует).
- [x] **Page source**: `canonical = https://era2.ai/`, JSON-LD `FAQPage` есть, `BreadcrumbList` — НЕТ.
- [x] **Битые картинки**: 404 по `/models/*.jpg` и `/community/*.jpg` — не зафиксировано (0 на `/` и 0 на `/ai/image`).

## Итог

Главная полностью переведена на SEO-конструктор. Все пункты чек-листа выполнены, регрессий не обнаружено.